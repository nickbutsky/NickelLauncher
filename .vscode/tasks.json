{
	"tasks": [
		{
			"command": "npx vite build",
			"label": "preLaunch",
			"type": "shell"
		},
		{
			"command": "Remove-Item -Recurse -Force build",
			"label": "postDebug",
			"type": "shell"
		}
	],
	"version": "2.0.0"
}
